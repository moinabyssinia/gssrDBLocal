<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tide Gauges</title>
</head>
<body>
    <% if (country === undefined) { %> 
        <% country = "ALL" %> 
    <% } %> 

    <h1> <%= country.toUpperCase() %>  Tide Gauges</h1>
    
    <h3> <%= tgs.length %> tide gauges found  </h3>

    <% let ii = 1; %> 
    <% for (let tg of tgs) { %> 
        <p> <%= ii %>. <a href="/twcr/<%= tg._id %> "><%= tg.name %></a> </p>
        <% ii = ii + 1; %> 
    <% } %> 

</body>
</html>